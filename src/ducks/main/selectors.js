export const selectWidth = state => state.width;
export const selectHeight = state => state.height;
export const selectLength = state => state.length;
export const selectResult = state => state.result;